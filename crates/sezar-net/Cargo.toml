[package]
name        = "sezar-net"
version     = { workspace = true }
edition     = { workspace = true }
rust-version = { workspace = true }
license     = { workspace = true }
description = "Sezar network module — TLS / SSH / IPsec ciphersuite observation via eBPF"

[lib]
name = "sezar_net"
path = "src/lib.rs"

[dependencies]
sezar-core = { workspace = true }
serde      = { workspace = true }
tokio      = { workspace = true }
tracing    = { workspace = true }
anyhow     = { workspace = true }
chrono     = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
# eBPF binding (aya) wiring lands in #SEZ-3. Until then this crate is
# a typed stub so the workspace builds end-to-end.
