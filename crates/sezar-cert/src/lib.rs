//! `sezar-cert` — X.509 inventory module.
//!
//! Reserved crate. Real implementation begins in V2 — see
//! `ROADMAP.md`. Until then this is an empty placeholder so the
//! workspace builds and CI is consistent across milestones.

/// Module identifier emitted as `source_module` on every event once
/// V2 lands.
pub const MODULE_NAME: &str = "sezar-cert";
